# Canonical absolute workbench artifact root outside Git. Store public-model
# caches, generated audio, and benchmark outputs here.
TTS_WORKBENCH_ARTIFACT_ROOT=

# Optional absolute path outside the repository. One literal private identifier
# per line for local scanning; never commit this file or its contents.
TTS_WORKBENCH_PII_DENYLIST=
